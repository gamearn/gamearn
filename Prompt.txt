Go with: separate role-gated admin module with its own nav. Yes, build all 5 standalone sub-screens. Yes, add Sell Coins next to Buy Coins in wallet. Start building the 16 missing screens now — retry the Figma divergence check on the other 8 once rate limits reset, don't block on it. Build order: standalone sub-screens first (quick wins), then wallet Sell Coins, then admin module last since it's the biggest chunk.